---
layout: layouts/base.liquid
name: "Brendan Davies"
title: "Brendan Davies’s Résumé"
subtitle: "Software Engineer"
description: "Brendan Davies is [DOING IMPRESSIVE AND WONDERFUL THINGS]."
summary: / Pet Dad / Woodworker
---

<header>
  <h1>{{ name }}</h1>
  <h2>{{ subtitle }}</h2>
</header>

<main>
  <section class="contact">
    <header class="small">
      <h1>Contact</h1>
    </header>
    <address>Philadelphia</address>

    <ul>
      {% for item in contact.items %}
      <li><a href="{{ item.link }}">{{ item.name }}</a></li>
      {% endfor %}
    </ul>
  </section>

  <section class="summary">
    <header class="small">
      <h1>Summary</h1>
    </header>
    <p>{{ summary }}</p>
  </section>

  <section class="experience">
    <header>
      <h1>Experience</h1>
    </header>

    <dl>
      {% for item in experience.items %}
      <dt>
        <h2>{{ item.company }}
          {% for position in item.positions %}
          <h3>{{ position.title }}
            <small>{{ position.dates }}</small>
          </h3>
          {% endfor %}
      </dt>
      <ul>
        {% for point in item.details %}
          <li>{{ point }}</li>
        {% endfor %}
      </ul>
      {% endfor %}
    </dl>
  </section>

  <section class="education">
    <header>
      <h1>Education</h1>
    </header>
    <dl>
      {% for item in education.items %}
      <dt>{{ item.name }}</dt>
      <dd>{{ item.description }}</dd>
      {% endfor %}
    </dl>
  </section>

  <section class="skills">
    <header>
      <h1>Skills</h1>
    </header>
    <ul>
      {% for item in skills.items %}
      <li>{{ item.name }}</li>
      {% endfor %}
    </ul>
  </section>
</main>